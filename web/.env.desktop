# Build-Profil für die Electron-/Desktop-Variante (vite build --mode desktop).
# Schaltet das Frontend in den local mode: kein Keycloak, fester lokaler Admin,
# API-Basis kommt zur Laufzeit aus window.__ERATO__ (von Electron injiziert).
VITE_LOCAL_MODE=1
